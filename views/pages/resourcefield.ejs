<!DOCTYPE html>
<html>

<head>
    <title>
        <%= title %> - Level: <%= resourceLevel %>
    </title>
</head>

<body>

    <%= title %> level: <%= resourceLevel %>
            <br>
            <br>
            The <%= title.toLowerCase() %> provides additional resources for your town.
                <br> <br>
                <% if (resourceLevel <=19){ %>
                    <form action="/land/<%= title.toLowerCase().replace(/ /g, '') %>/<%= resourceId %>/upgrade">
                        <input type="submit" value="Upgrade">
                    </form>

                    <% } else { %>
                        The <%= title.toLowerCase() %> is already at max level!
                            <% } %>

</body>

</html>