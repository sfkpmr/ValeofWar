<!DOCTYPE html>
<html>

<head>
    <title>
        Land
    </title>
</head>

<body>

    <% for (var i=0; i < farms; i++ ) { %>
        <a href="/farm/<%= i %>">Farm</>
    <% } %>

    <br>

    <% for (var i=0; i < goldMines; i++ ) { %>
        <a href="/goldMine/<%= i %>">Gold mine</>
    <% } %>

    <br>

    <% for (var i=0; i < ironMines; i++ ) { %>
        <a href="/ironMine/<%= i %>">Iron mine</>
    <% } %>

    <br>

    <% for (var i=0; i < lumberCamps; i++ ) { %>
        <a href="/lumbercamp/<%= i %>">Lumber camp</>
    <% } %>
    
    <br>

    <% for (var i=0; i < quarries; i++ ) { %>
        <a href="/quarry/<%= i %>">Quarry</>
    <% } %>

    <br>
    <br>
    <a href="/town">Town</a> <a href="/land">Land</a> <a href="/logout">Log out</a>

</body>

</html>